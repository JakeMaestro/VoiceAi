[general]
static=yes
writeprotect=no

[incoming]
exten => _X.,1,NoOp(VoiceAI: Call received for ${EXTEN})
 same => n,Answer()
 same => n,NoOp(Sending call to VoiceAI Orchestrator)
 same => n,Stasis(voiceai)     ; Übergibt Call in die Orchestrator Logik
 same => n,Hangup()
